<Window x:Class="WpfApp.Window2_Class_"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="Window2_Class_" Height="450" Width="912">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Danh sách Lớp học" FontSize="26" FontWeight="Bold"/>
                <TextBlock Text="Quản lý lớp học" FontSize="12"/>
            </StackPanel>

            <Button Content="+ Tạo lớp học mới"
                HorizontalAlignment="Right"
                Padding="15,5"
                Background="#4F46E5"
                Foreground="White"
                Click="BtnAdd_Click"
                />
        </DockPanel>

        <!-- SEARCH -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="txtSearch" Width="250" Height="30" Margin="0,0,10,0"/>
            <ComboBox x:Name="cbStatus" Width="150" Height="30" SelectedIndex="0">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Open"/>
                <ComboBoxItem Content="Closed"/>
            </ComboBox>
            <Button Content="Search" Width="80" Margin="10,0,0,0" />
        </StackPanel>

        <!-- DATAGRID -->
        <DataGrid x:Name="dgClasses"
              Grid.Row="2"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              RowHeight="45"
              SelectionMode="Single">

            <DataGrid.Columns>

                <!-- Class Code -->
                <DataGridTextColumn Header="MÃ LỚP"
                                Binding="{Binding ClassCode}"
                                Width="120"/>

                <!-- Course + Teacher -->
                <DataGridTemplateColumn Header="THÔNG TIN" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding CourseId, StringFormat=Course ID: {0}}"/>
                                <TextBlock Text="{Binding TeacherId, StringFormat=Teacher ID: {0}}"
                                       FontSize="11"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Date -->
                <DataGridTemplateColumn Header="THỜI GIAN" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding StartDate, StringFormat=dd/MM/yyyy}"/>
                                <TextBlock Text=" - "/>
                                <TextBlock Text="{Binding EndDate, StringFormat=dd/MM/yyyy}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Capacity -->
                <DataGridTextColumn Header="SĨ SỐ"
                                Binding="{Binding Capacity}"
                                Width="100"/>

                <!-- Status -->
                <DataGridTextColumn Header="TRẠNG THÁI"
                                Binding="{Binding Status}"
                                Width="100"/>

                <!-- Action -->
                <DataGridTemplateColumn Header="THAO TÁC" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">

                                <Button Content="Sửa"
                                    Margin="0,0,5,0"
                                    Click="BtnEdit_Click"/>

                                <Button Content="Xóa"
                                    />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
